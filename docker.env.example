# Docker Environment Variables for Python Remove Background Provider

# Server Configuration
HOST=0.0.0.0
PORT=8001

# Logging Configuration
LOG_LEVEL=INFO

# Optional: Custom model download directory (default: ~/.u2net)
# REMBG_HOME=/app/models

# Optional: Maximum file size in bytes (default: 10MB)
# MAX_FILE_SIZE=10485760

# Optional: Allowed file types (comma-separated)
# ALLOWED_EXTENSIONS=jpg,jpeg,png,bmp,tiff,webp

# Production settings
# NODE_ENV=production





